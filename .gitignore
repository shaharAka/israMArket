.DS_Store
.env
.env.local
.env.*.local
*.pyc
__pycache__/
.venv/
venv/
api/data/
node_modules/
.next/
out/
dist/
*.db
*.sqlite
.turbo/
.vercel
next-env.d.ts
*.tsbuildinfo

# Design-review artifacts: local screenshots and exported card PNGs.
.design-review/

# Local-only fixtures for the internal /dev/cards preview page. Deliberately not
# committed: real-photo.webp is a third party's photograph, and the AI plate is a
# throwaway generation. Re-create them locally to use the preview page.
web/public/dev-preview/

# Local dev runtime: a self-contained Python install and a workspace-local npm cache,
# plus browser-verification scripts and screenshots. None of it belongs in the repo.
.runtime/
.npm-cache/
